import { PlaceholderView } from "@/components/Empresa/PlaceholderView"

export default function RolesPage() {
  return (
    <PlaceholderView
      seccion="Administración"
      titulo="Roles"
      descripcion="Define permisos y niveles de acceso para cada miembro del equipo."
    />
  )
}
